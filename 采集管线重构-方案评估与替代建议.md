# 《ArtPortal 采集管线重构实施计划》方案评估与替代建议

> 针对桌面上那份 DeepSeek 生成的实施计划的独立评估。所有结论基于对代码库的实际核查与线上数据实测,
> 不接受计划文档的自述前提。2026-08-03 产出,配套现状盘点见 [AUDIT.md](AUDIT.md)。

## 总评

**方向对,前提错,执行方案约 60% 在重建已有能力,另有 4 处会让现有能力倒退。**

这份计划的作者没有看过代码库(你确认是 DeepSeek 凭描述生成的),所以它是一份**教科书式的通用内容
聚合器架构**——单看架构本身是合格的,但它把 ArtPortal 当成一个尚未开工的空项目来设计。实际情况是:
它提出要建的东西,项目里大部分已经在跑,而且有几处现有实现**严格强于**计划的方案。

建议:**采纳它的诊断方向和其中 3 个具体想法,不要按它的阶段划分施工。**

---

## 一、值得采纳的部分(计划真正的价值)

### ✅ 1. 核心诊断成立
> "搜索引擎优先索引高外链高流量页面,而艺术机会公告恰恰是低流量、几乎无外链、发布后数周即失效的
> 长尾页面。同样的 query 永远返回同一批 SEO 良好的聚合站,因此新增趋近于零。"

**这个机理判断是对的**,也是这份文档最有价值的一句话。搜索对"反复发现同一批机会"确实低效。
(但由此推出的"新增趋近于零"是错的,见第二节。)

### ✅ 2. 邮件订阅通道 —— 全文最好的想法,应当采纳
e-flux announcements、ResArtis、TransArtists、ArtRabbit 这些**才是艺术圈真正的分发渠道**。机会是
"推"到订阅者收件箱的,不是"拉"下来的。项目现在完全没有这条通道(全仓库无 IMAP/收信代码,
`mailer.mjs` 只能发信)。

这一条单独拿出来做,价值就超过计划里其余所有条目的总和。

### ✅ 3. 源产出分级(yield_count 驱动)
按历史产出自动升降源的轮询频率,把预算花在真出货的源上。现有 `sources.json` 没有这个字段,
`regions-report.json` 有类似思路但只覆盖区域经理。值得做,但**是给现有 sources.json 加字段,不是新建表**。

### ✅ 4. 零结果查询反哺补源
用户搜了没结果 → 说明该类源缺失 → 指导补源方向。成本极低,信号很实。现有 `search_ingest` 表
(102条)已经在记"检索带进来了什么",补记"检索什么都没带进来"是自然延伸。

---

## 二、事实性错误:计划的前提站不住

### ❌ 1. "每天新增机会极少,新增趋近于零" —— 与实测不符

线上 `opportunities.json` 按 `first_seen` 统计的真实日新增:

| 日期 | 新增 |
|---|---|
| 2026-07-30 | 38 |
| 2026-07-31 | 18 |
| 2026-08-01 | 14 |
| 2026-08-02 | 5 |
| 2026-08-03 | 8 |

日均约 5–38 条。对单人项目来说不算高,但**绝不是"趋近于零"**。整个计划的紧迫性论证建立在一个
夸大的前提上。

### ❌ 2. 真正的病因被漏掉了:过期挤占

线上 **686 条机会,只有 286 条未过期(58% 已过期)**。

用户感知的"没有新内容",主要不是采集不出来,而是**库里六成是过期的**。这个问题用一个定时归档
任务就能解决,零模型成本、一天工作量,见效比任何采集架构改造都快。计划把它排在阶段 2 的第 4 小
条,严重低估。

### ❌ 3. 砍掉 65% 的产能,却没有替代方案

线上数据来源分布:

| 来源 | 条数 | 占比 |
|---|---|---|
| `_via=search`(检索类路径) | 446 | **65%** |
| 固定信源每日管线 | 240 | 35% |

计划要求"不要保留或新增任何全网搜索式的采集逻辑",等于砍掉贡献 65% 存量的路径,而指望 202 个
固定源顶上——这 202 个源积累数月才产出 240 条。**执行后新增量会先断崖再慢慢爬**,计划对此只字未提。

---

## 三、重复造轮子:计划要建的,项目已经在跑

| 计划条目 | 现状 | 位置 |
|---|---|---|
| 哈希比对,未变更则跳过(成本闸门) | **已有**,且详情页也逐页哈希 | `run.mjs:92-97`、`:112` |
| 人工审核队列 review_queue | **已有**,今天刚生成 17 条待审 | `run.mjs:211` → `state/review-queue.json` |
| 源注册表 sources | **已有 202 条**,含 type/domain/region_hint/category_hint/robots/reachable/org_type | `sources.json` |
| 结构化抽取 + 严格 JSON schema | **已有** | `lib/extract.mjs` + `prompts/extract.txt` |
| 去重 | **已有** | `lib/dedupe.mjs` |
| 过期不入库 | **已有**(v0.98.0 加的入口闸) | `verify.mjs:78-90` |
| 礼貌抓取 / robots | **已有** | `lib/fetch.mjs` + `lib/robots.mjs` |
| 源投稿入口 | **已有**(submissions 表 + 统一发布系统) | `lib/db.mjs`、v0.72.0 |

按计划施工,阶段 1–3 里约 60% 的工作量是把上面这些再写一遍。

---

## 四、会导致能力倒退的 4 处(最需要警惕)

### 🔴 1. 反幻觉方案是降级,而且踩过的坑要重踩

**计划的方案**(附录B):在 prompt 里写"任何无法从原文直接确认的字段,一律输出 null;禁止推测"。

**项目现有方案**(`lib/verify.mjs`,注释原话"这是整个系统的支点"):AI 必须为每个关键字段附上
`evidence` 原文片段,然后**程序**校验该 evidence 是不是原文的子串——不是就把该字段作废置 null,
并写进 `hallucination.log`。标题 evidence 过不了整条存疑,跨域名 URL 整条丢弃。

后者严格强于前者:前者是**请求**模型别编,后者是**代码验证**模型没编。

而且这不是理论问题——项目记忆里已经有两次实测教训:「弱模型不听提示词是常态,凡靠提示词约束处
都要代码兜底」(周刊配图 GLM 完全无视配图指令、画室评级弱视觉模型全打B,改提示词无效)。
**按计划把支点从代码校验换成 prompt 约束,是拿已经付过学费的教训去换一个更弱的方案。**

### 🔴 2. 数据 schema 是回退,照迁会拆掉现有产品功能

计划的 `opportunities` 表结构丢掉了:
- `disciplines` —— 23 门类标签,v0.66.0 的核心产品功能(全站门类筛选、卡片彩标)
- `title_zh` / `title_en` 等双语字段 —— 全站中英切换依赖它
- `evidence` 字段 —— 反幻觉的支点
- `trust` 分级(auto/pending/verified)—— 换成了扁平的 status

同时前端 `site/index.html:3331` 是直接 `d3.json('data/opportunities.json')` 拉静态文件的,计划
自己也写了"不要重写网站前端"——但它的 schema 迁移会直接让前端读不到需要的字段。**两条要求自相冲突。**

### 🔴 3. 计划自己违反了项目第一红线

计划一边在"明确不要做的事"里写 **"不要让模型在字段缺失时合理推测"**,一边在阶段 2 要求:

> "所有 deadline 统一转 UTC 存储……若原文未标注时区,**按主办方所在地时区推断**"

艺术征集的截止日期,原文绝大多数就是一个光秃秃的本地日期("June 21"、"2026年6月21日")。
强行转 UTC 需要补上原文根本没有的"几点截止"和"哪个时区"——**这是在制造原文不存在的精度**,
正好撞上项目红线 1「AI 只整理抓来的原文,绝不编造」。

现有 `deadline` + `deadline_note`(保留原文表述)的设计是对的,不该动。

### 🔴 4. 会砍掉真正的复利引擎

计划:"不要保留或新增任何全网搜索式的采集逻辑"。这会把 `discover-sources.mjs` 一起砍掉。

但**搜索有两种用途,计划把它们混为一谈了**:
- 搜**机会** → 重复、低效、永远返回同一批聚合站(计划批评得对)
- 搜**信源** → 一次搜索花费换一个永久免费的信源,**有复利**(计划没意识到)

项目已经想通了这一层(路线图第 30 项就是"地区机构信源扩量",v0.99.0 已用它新增 27 个真实机构)。
计划的一刀切会砍掉这个正确的东西。

---

## 五、更好的方案:实测支撑的技术路线

我抽样探测了 26 个真实源站(国内 13 / 国际 13),验证计划的技术假设:

| 能力 | 国内源 | 国际源 | 判断 |
|---|---|---|---|
| RSS / Atom | 0/13 | 1/13 | **计划设想的 RSS 通道基本不存在**。也证伪了我自己"很多站有RSS只是没登记"的假设 |
| sitemap.xml | 2/13 | **10/13** | 国际源的最优解;带 `lastmod` 的可精确判断哪一页变了 |
| ETag / Last-Modified | **8/13** | 3/13 | 国内源的最优解,304 直接不下载正文 |
| JSON-LD 结构化数据 | 0/13 | 2/13 | 太稀少,不值得单独建通道 |

→ **结论:不该用计划里"一套 web-diff 打天下"的做法,应当分区差异化。**

### 比计划更优的三个技术点

**① 国际源走 sitemap + lastmod,跳过整个"列表页发现"环节**
现在的流程是「抓列表页 → `discoverDetailLinks` 猜详情链接 → 逐个抓详情页算哈希」,`hashes.json`
里 528 个详情页哈希就是这么来的,其中大量是无效抓取。10/13 的国际源有 sitemap,**一个请求就能
知道哪些页面在什么时候变了**,直接抓那几页。这比计划的"抓页面→提正文→算哈希"精确得多也便宜得多。

**② 国内源走条件请求(If-Modified-Since / If-None-Match)**
8/13 国内源支持。计划的哈希闸门是"**先下载完整页面**再算哈希决定要不要调模型"——省的是模型钱。
条件请求是"**服务器直接回 304,正文根本不下载**"——省的是带宽。而项目记忆里明确记着:
**服务器真正的瓶颈是出口带宽约 20Mbps,不是 CPU**。所以这一层比计划的哈希闸门更贴合真实约束。
(两者不冲突,条件请求在哈希之前,叠加使用。)

**③ 邮件通道要接进现有管线,不是新建一套**
计划把邮件抽取写成独立链路。但 e-flux/ResArtis 这类是**第三方聚合站**,而项目有"官网必达"硬闸
(`isThirdParty` → `locateOfficial`,v0.98.1 刚找回过一次)。邮件里抽到的机会必须同样走
`locateOfficial` 定位主办方官网 + `verifyRecord` evidence 校验,否则等于给聚合站开后门,
把刚补上的红线又拆掉。

---

## 六、我建议的执行顺序(按 ROI 排,与计划的阶段划分不同)

| 优先级 | 事项 | 工作量 | 为什么排这里 |
|---|---|---|---|
| **P0** | **过期治理**:686条里400条过期,做定时归档 + 前台彻底剔除 | 1天,零模型成本 | 用户感知"没新内容"的首要原因。比任何采集改造见效都快 |
| **P1** | **邮件订阅通道**(IMAP 收信 → 解析 → 接入现有 `extract`+`verify`+`locateOfficial` 管线) | 3–5天 | 计划最好的想法。艺术圈真正的分发渠道,项目完全空白 |
| **P2** | **sources.json 加产出字段**(yield_count/fail_count/tier/last_polled/last_hash)+ 按产出调频 | 1–2天 | 计划的好想法,但是加字段不是新建表 |
| **P3** | **抓取效率改造**:国际源 sitemap+lastmod、国内源条件请求 | 2–3天 | 实测支撑,省带宽(真瓶颈)+ 提高变更检出精度 |
| **P4** | **检索路径改造**:4条路径(用户检索/自动检索/区域经理/探长)的写库出口统一改成"进待审队列",**保留发现能力,去掉直接公开** | 2天 | 比计划的"全砍"稳。既解决"检索不该直接写库",又不丢 65% 产能 |
| **P5** | 零结果查询记录 + 补源看板 | 1天 | 便宜的长期信号 |

**明确不做**:计划的 schema 迁移(第四节🔴2)、UTC 时区转换(🔴3)、把 evidence 校验换成 prompt
约束(🔴1)、砍掉 `discover-sources.mjs`(🔴4)、引入 Claude Haiku 付费抽取(与既定 GLM 免费档
主力策略 + token 硬约束冲突)。

---

## 七、一句话结论

这份计划**值得读,不值得照做**。它的诊断方向和邮件订阅这个点子是真金,但它不了解代码库,
把已有的能力当成待建的,把更强的实现(代码级 evidence 校验)换成更弱的(prompt 约束),
还在自己的红线上绊了一跤(UTC 推断)。

按上面 P0–P5 走,能拿到它承诺的全部收益,而且不用拆掉现有的东西。
