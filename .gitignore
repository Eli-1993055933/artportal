# 依赖
node_modules/

# 密钥 —— 绝不入库(API key 只放 GitHub Secrets)
.env
.env.*
*.key

# pipeline 运行期产物(样本/临时;状态文件按需保留)
pipeline/state/samples/
pipeline/state/demo-*.json
pipeline/state/offline-*.json

# 账号系统数据 —— 含用户邮箱 / 口令哈希 / 会话令牌 / IP,绝不入库(PII)
pipeline/state/users.json
pipeline/state/sessions.json
pipeline/state/events.jsonl*

# 翻译回滚备份
site/data/opportunities.json.bak-*

# 系统
.DS_Store
Thumbs.db
*.log
!pipeline/state/hallucination.log

# 部署打包产物
artportal-site.zip
*.zip
site/assets/covers/
pipeline/state/pagetrans.json*
site/assets/avatars/
site/assets/works/
