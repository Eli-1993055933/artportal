# ArtPortal 项目进度与下一步(交接文档)

> 随时开新对话时,先让 AI 读这份文件 + `README.md` + `DEPLOY.md`,就能接上。
> 项目根目录:`D:\Claude Code`,已 git 管理(master 分支)。

## 一句话现状
网站本体已做完(64 条真实机会、封面、真官网链接、来源标注、logo、主视觉),
每日自动抓取管道已跑通(DeepSeek),现在卡在**部署上线**——正在办阿里云 ICP 备案。

## 已完成 ✅
- **前端** `site/`:纯静态,手机/电脑三端适配;搜索/筛选/详情/收藏/分页;封面图(64/64);
  来源标注(官网直采 / 转自某聚合平台);"前往官网"已联网校正到真实主办方官网(44 条)。
- **数据** `site/data/opportunities.json`:64 条,全部真实抓取 + evidence 校验,非 AI 编造。
- **管道** `pipeline/`:抓取(robots+限速+署名UA+哈希)→ DeepSeek 提取(强制逐字 evidence)
  → 程序校验 → 去重 → 信任分级 → 封面 → 报告。信源 28 个。
- **每日自动**:Windows 任务计划「ArtPortal每日抓取」每天 6:00 跑 `pipeline/run-daily.bat`。
  DeepSeek key 在 `pipeline/.env`(已 gitignore,不入库)。
- **品牌**:logo「观测站」(空间站俯瞰地球)+ favicon + 横版锁定 + 深色主视觉,在 `site/assets/`。
- **部署包**:根目录 `artportal-site.zip`(拖去托管即可;改了代码用 PowerShell 重打:
  `Compress-Archive -Path "site\*" -DestinationPath "artportal-site.zip" -Force`)。

## 部署进度 🔵(正在这里)
- 域名:**artportal123.com**(阿里云买,已**域名实名认证成功**)
- 服务器:阿里云**轻量应用服务器**,2核2G,宝塔面板,北京,1年;公网 IP **60.205.212.195**
- **ICP 备案:填写中**。网站名称用「**创作机会手记**」(个人备案,避开"艺术/全球/平台"等词)。
  下一步就是把备案填完提交 → 阿里云初审(1-2天)→ 管局审核(3-20天,会来核验电话)。

## 备案通过后的上线步骤 ⏳
1. 宝塔面板(浏览器开 `http://60.205.212.195:8888`,首次要在服务器控制台拿宝塔登录信息)
   → 新建站点 → 把 `site/` 里的文件上传到网站根目录。
2. 域名解析:阿里云云解析 DNS(免费版)加一条 **A 记录**,把 artportal123.com 指向 60.205.212.195。
3. 宝塔里一键申请 **SSL 证书**(免费),开启 HTTPS。
4. 页脚加上 **ICP 备案号**链接(备案通过后会给号,链接到 beian.miit.gov.cn)——法规要求。

## 你只需要碰的 5 个概念
域名 + 备案 + 轻量服务器 + DNS + SSL。其余(RDS/OSS/CDN/ECS…)阿里云推销的都不用。

## 还没做的小事(上线前可补)
- "提交机会"按钮是占位:接个腾讯问卷/金数据表单,链接填进 `site/js/app.js` 的 `SUBMIT_FORM_URL`。
- "信息有误"mailto 用的是 atsang799@gmail.com,公开后可换专用邮箱。
- 少数封面重复(已知,暂不处理)。
